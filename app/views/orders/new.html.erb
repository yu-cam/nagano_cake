<h1>注文情報入力</h1>

<h4>支払い方法</h4>
<%= form_with model: @order, url:orders_confirm_path do |f| %>
  <label><%= f.radio_button :payment_methot, 0 %> クレジットカード </label>
  <label><%= f.radio_button :payment_methot, 1 %> 銀行振り込み</label>
  <%= f.submit %>
<% end %>

 <h4>お届け先</h4>
　<%= form_for(@adress, url:adresses_path) do |f| %>
  <label><%= f.radio_button :address, %>"ご自身の住所"</label>
    <%= customer.adress %>
    <%= name %>
  <label><%= f.radio_button :address, %>"登録済みの住所から選択"</label>
    <%= f.text_field :address %>
    <% end %>

  <%= form_for(@address, url:addresses_path) do |f| %>
  　<label><%= f.radio_button :address, %>"新しいお届け先"</label>
      <h4>郵便番号 ハイフンなし </h4><%= f.text_field :postal_code %>
      <h4>住所</h4><%= f.text_field :address %>
      <h4>宛名</h4><%= f.text_field :name %>

      <button type="submit" class="btn btn-primary mt-1">確認へ進む</button>
　　 <% end %>