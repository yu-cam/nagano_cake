<div class="container">
  <div class="row">
  	<div class="col-xs-5">
  	<h4>ジャンル編集</h4>
    <%= form_for(@genre, url: admin_genre_path(@genre)) do |f| %>
    <p>ジャンル名</p>
    <%= f.text_field :name %>
    <%= f.submit "変更を保存", class: "btn-sm btn-success" %>
    <% end %>